#Generated by nginxconfig.io
# http://localhost:3000/#?0.domain=register.anthonybible.com&0.path=%2Fvar%2Fwww%2F&0.document_root=%2Fhtml&0.non_www=false&0.https=false&0.force_https=false&php_server=%2Fvar%2Frun%2Fphp%2Fphp7.3-fpm.sock

user nginx;
pid /run/nginx.pid;
worker_processes auto;
worker_rlimit_nofile 65535;

events {
	multi_accept on;
	worker_connections 65535;
}

http {
	server_names_hash_bucket_size 64;
	charset utf-8;
	sendfile on;
	tcp_nopush on;
	tcp_nodelay on;
	server_tokens off;
	log_not_found off;
	types_hash_max_size 2048;
	client_max_body_size 16M;

	# MIME
	include mime.types;
	default_type application/octet-stream;

	# logging
	access_log /var/log/nginx/access.log;
	error_log /var/log/nginx/error.log warn;

	# load configs
	include /etc/nginx/conf.d/*.conf;
	include /etc/nginx/sites-enabled/*;
}
# ---------------------------------------------------------
# default 
# ---------------------------------------------------------
